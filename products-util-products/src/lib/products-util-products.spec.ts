import { productsUtilProducts } from './products-util-products';

describe('productsUtilProducts', () => {
  it('should work', () => {
    expect(productsUtilProducts()).toEqual('products-util-products');
  });
});
