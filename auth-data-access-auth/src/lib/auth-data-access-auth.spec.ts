import { authDataAccessAuth } from './auth-data-access-auth';

describe('authDataAccessAuth', () => {
  it('should work', () => {
    expect(authDataAccessAuth()).toEqual('auth-data-access-auth');
  });
});
