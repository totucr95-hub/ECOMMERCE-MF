import { productsDomainProducts } from './products-domain-products';

describe('productsDomainProducts', () => {
  it('should work', () => {
    expect(productsDomainProducts()).toEqual('products-domain-products');
  });
});
